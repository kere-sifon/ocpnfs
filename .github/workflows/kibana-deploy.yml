name: Kibana Deploy

on:
  workflow_dispatch:
    inputs:
      action:
        description: 'Action to perform'
        required: true
        type: choice
        options:
          - deploy
          - teardown
        default: 'deploy'

env:
  CHART_PATH: ./kibana-chart
  RELEASE_NAME: kibana

jobs:
  kibana:
    runs-on: oc-run
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Login to OpenShift
        run: |
          echo "Logging in to OpenShift..."
          if [ -z "${{ secrets.OPENSHIFT_TOKEN }}" ]; then
            echo "Error: OPENSHIFT_TOKEN secret is not set"
            exit 1
          fi
          if [ -z "${{ secrets.OPENSHIFT_SERVER }}" ]; then
            echo "Error: OPENSHIFT_SERVER secret is not set"
            exit 1
          fi

          oc login --token=${{ secrets.OPENSHIFT_TOKEN }} \
            --server=${{ secrets.OPENSHIFT_SERVER }} \
            --insecure-skip-tls-verify=true
          oc project bell-test

          oc whoami
          echo "✓ Successfully logged in to OpenShift"

      - name: Verify Elasticsearch is Running
        if: github.event.inputs.action != 'teardown'
        run: |
          echo "Verifying Elasticsearch is available..."
          ES_POD=$(oc get pod -l app=elasticsearch -n bell-test -o jsonpath='{.items[0].metadata.name}' 2>/dev/null || echo "")
          if [ -z "$ES_POD" ]; then
            echo "Error: Elasticsearch is not running. Deploy elasticsearch-chart first."
            exit 1
          fi
          echo "✓ Elasticsearch found: $ES_POD"

      - name: Clean up existing resources
        if: github.event.inputs.action != 'teardown'
        run: |
          echo "Cleaning up existing Kibana resources..."
          oc delete deployment kibana --ignore-not-found=true -n bell-test
          sleep 5
          echo "✓ Cleanup completed"

      - name: Deploy Kibana
        if: github.event.inputs.action != 'teardown'
        run: |
          echo "=== Deploying Kibana ==="

          helm upgrade --install ${{ env.RELEASE_NAME }} ${{ env.CHART_PATH }} \
            --set namespace=bell-test \
            --timeout=10m \
            --wait

          echo "✓ Kibana deployed successfully"

      - name: Wait for Kibana Ready
        if: github.event.inputs.action != 'teardown'
        run: |
          echo "Waiting for Kibana to be ready..."
          oc rollout status deployment/kibana \
            -n bell-test \
            --timeout=300s
          echo "✓ Kibana is ready"

      - name: Validate Kibana
        if: github.event.inputs.action != 'teardown'
        run: |
          echo "=== Validating Kibana ==="
          KIBANA_POD=$(oc get pod -l app=kibana -n bell-test -o jsonpath='{.items[0].metadata.name}')
          echo "Kibana pod: $KIBANA_POD"

          echo "--- Kibana Status ---"
          oc exec $KIBANA_POD -n bell-test -- \
            curl -s http://localhost:5601/api/status | grep -o '"level":"[^"]*"' | head -5 || true

          echo "✓ Kibana validation complete"

      - name: Get Kibana Route
        if: github.event.inputs.action != 'teardown'
        run: |
          echo "=== Kibana Access ==="
          KIBANA_URL=$(oc get route kibana -n bell-test -o jsonpath='{.spec.host}' 2>/dev/null || echo "No route found")
          if [ "$KIBANA_URL" != "No route found" ]; then
            echo "✓ Kibana available at: https://$KIBANA_URL"
          else
            echo "Note: No route configured. Access via service or create route manually."
          fi

      - name: Debug on failure
        if: failure()
        run: |
          echo "=== Debugging Kibana ==="
          oc get pods -n bell-test -l app=kibana
          echo ""
          oc describe deployment kibana -n bell-test || true
          echo ""
          oc logs deployment/kibana -n bell-test --tail=50 || true
          echo ""
          oc get events -n bell-test --sort-by='.lastTimestamp' | tail -20

      - name: Teardown Kibana
        if: github.event.inputs.action == 'teardown'
        run: |
          echo "=== Tearing Down Kibana ==="
          helm uninstall ${{ env.RELEASE_NAME }} -n bell-test || echo "Release not found"
          echo "✓ Teardown completed"

      - name: Deployment Summary
        if: always() && github.event.inputs.action != 'teardown'
        run: |
          echo "=== Kibana Deployment Summary ==="
          echo "Namespace: bell-test"
          echo "Release: ${{ env.RELEASE_NAME }}"
          echo ""
          oc get deployment,pod,svc,pvc \
            -l app=kibana \
            -n bell-test || true
